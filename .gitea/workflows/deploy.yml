name: Deploy Tars
on:
  push:
    branches: [ "main" ] # Solo se activa cuando algo llega a main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Docker Compose Up
        run: docker compose up -d --build
