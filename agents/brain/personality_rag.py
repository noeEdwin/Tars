# agents/brain/personality_rag.py



def append_style_examples(last_user_msg: str, feedback_type: str, protocol_text: str) -> str:
    """
    TODO (Step 2.3): Re-implement RAG-based personality injection.
    Previously imported app_state["style_library"] which was never populated (dead code).
    """
    return protocol_text